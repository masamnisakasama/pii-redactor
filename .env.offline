# .env.offline

#app/.env

# ====== Active / Minimal & Safe ======

# CORS
FRONTEND_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# セキュリティレベル（Settings.default_security_level は ENV 名が UPPER の想定）
DEFAULT_SECURITY_LEVEL=maximum
DISABLE_NETWORK_ACCESS=true

# 置換・エイリアス
TENANT_HMAC_KEY_B64=/G1KnwKEu2g4fJQ0rPo8B0jZUB5wYM5ymQy5SbX8HC0=
PII_IMAGES_MODE=pixelate
PII_POLICY=email,phone,address,id,face
PII_CONSISTENCY=site-2025

# パフォーマンス
MAX_IMAGE_SIZE=1600
PDF_DPI=150
MAX_FILE_SIZE_MB=20
ALLOWED_FILE_EXTENSIONS=pdf,png,jpg,jpeg

# EAST（重いのでまずOFF）
USE_EAST_TEXT=false

# 顔検出（完全ローカル）
USE_YUNET=false
USE_FACE_SEG=false
FACE_DETECTION_METHOD=haar
FACE_CASCADE_SCALE_FACTOR=1.2
FACE_CASCADE_MIN_NEIGHBORS=7
FACE_MIN_SIZE_PX=56


# ====== Archived (kept for reference; commented out) ======

# FRONTEND_ORIGINS=http://localhost:3000,http://localhost:8080
# TENANT_HMAC_KEY_B64=your_base64_encoded_hmac_key_here
# CONFIDENCE_THRESHOLD=0.80

# GOOGLE_API_KEY=
# NANOBANAN_API_KEY=
# NANOBANAN_ENDPOINT=
# NER_ENDPOINT=http://tfserving:8500/v1/models/ja_ner:predict

# OCR_CONFIDENCE_THRESHOLD=0.6
# TESSERACT_LANG=jpn+eng
# NER_CONFIDENCE_THRESHOLD=0.85

# MAX_IMAGE_SIZE=2048
# PDF_DPI=180
# MAX_FILE_SIZE_MB=50
# ALLOWED_FILE_EXTENSIONS=pdf,png,jpg,jpeg,gif,bmp,tiff

# SECRET_KEY=your-secret-key-here
# API_RATE_LIMIT=100

# （lower-case 版：pydantic BaseSettings の既定では ENV 名は UPPER を参照するため無効になりがち）
# default_security_level=maximum
# disable_network_access=true
# use_nanobanan_ocr=false
# use_nanobanan_ner=false

# OpenCV DNN / モデルパス類
# OPENCV_DNN_DIR=/Users/ikedamasahiro/work/faceswap-suite/pii-redactor/models/face
# YUNET_ONNX=models/face/face_detection_yunet_2023mar.onnx
# FACE_SEG_ONNX=models/seg/face_parsing_bisenet_512.onnx

# EAST（相当重いので一旦保留）
# USE_TEXT_EAST=true
# USE_EAST_TEXT=true
# EAST_PB=/app/models/text/frozen_east_text_detection.pb
# EAST_CONF_THRESHOLD=0.5
# EAST_NMS_THRESHOLD=0.4
# EAST_MAX_SIDE=1280
# EAST_MIN_SIZE=6

# 画像処理まわり
# PII_CONSENT_FACES=unknown
# USE_FACE_SEG=false

# （重複対策のためコメントアウト；後で使うなら Active セクションに移動）
# FRONTEND_ORIGINS=http://localhost:3000
# PII_IMAGES_MODE=mosaic

